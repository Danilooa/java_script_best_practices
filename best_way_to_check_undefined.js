/**
 * The best way to check if a variable is
 * undefined is to use typeof like the following 
 * example
 */
if (typeof variable_ === 'undefined') {
    console.log("variable_ is undefined");
}

var undefined_;
if (typeof undefined_ === 'undefined') {
    console.log("undefined_ is undefined");
}

